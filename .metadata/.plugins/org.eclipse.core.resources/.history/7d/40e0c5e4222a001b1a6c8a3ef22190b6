package task5;

public class SenderRule extends Rule{
	String senderName;
	public SenderRule(Command command, senderName) {
		super(command);
	}
	protected boolean check(Message msg) {
		return msg.getSender().equals(senderName);
	    }
}